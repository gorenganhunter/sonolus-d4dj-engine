export const slider = tutorialMemory({
    position: Number,
    isUsed: Boolean,
    prev: {
        beat: Number,
        lane: Number
    },
    next: {
        beat: Number,
        lane: Number
    }
})
